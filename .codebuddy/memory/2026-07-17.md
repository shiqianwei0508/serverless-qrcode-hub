# 2026-07-17

## Multi-language (i18n) completion & bilingual docs

Finished the multi-language task for `serverless-qrcode-hub`:

- **i18n validation**: wrote a one-off Node script (`_i18n_check.js`, deleted after) that checked `dist/i18n.js`
  dictionary coverage across all 8 languages (en/zh/ru/ja/ko/es/fr/de) and against every `I18N.t` /
  `data-i18n*` key used in `dist/admin.html` + `dist/login.html`.
  - Found and fixed 2 bugs:
    1. `admin.editTitle` and `admin.save` keys were referenced in `admin.html` (`#edit-modal`) but missing
       from `dist/i18n.js` → added to all 8 languages.
    2. `app.edit` (card row edit button) was used but only `admin.edit` existed → added `app.edit` to all 8 languages.
    3. German block was missing `app.reservedPath` / `app.invalidExpiry` / `app.invalidInput` and its closing
       brace was `}` not `},` → added the 3 keys and fixed the brace.
  - Final result: 121 keys × 8 languages, all frontend keys present. `index.js` `PUBLIC_I18N` (8 keys × 8 langs) already consistent.

- **Bilingual docs** (convention: English at canonical path, Chinese under `docs/zh/`):
  - `README.md` (EN) + `docs/zh/README.md` (ZH)
  - `README.v1.md` (EN) + `docs/zh/README.v1.md` (ZH)
  - `docs/CODE_DESIGN.md` (EN, full translation) + `docs/zh/CODE_DESIGN.md` (ZH, with i18n additions)
  - Cross-language links fixed to correct relative depths (docs/zh links use `../../` for root files).

- No commit/push performed (user convention: never push without explicit request).

## Project conventions confirmed
- Bilingual docs: English at canonical path, Chinese in `docs/zh/`. Link depth from `docs/zh/` to root files is `../../`.
- i18n keys: `en` is source of truth; every other language must contain the same key set.
