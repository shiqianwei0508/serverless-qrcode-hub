# 2026-07-16

- 完成 `docs/CODE_DESIGN.md` 代码设计文档编写（1101行），覆盖架构、后端 index.js、前端 login.html/admin.html、配置部署、安全优化建议。
- 已 commit `d8734c4`，未 push。
- UI 优化（专业商务风设计系统）：
  - 新增 `dist/theme.css`（覆盖 DaisyUI v5 主题变量：企业蓝 `--color-primary:#2563EB`、slate 中性灰阶、圆角/阴影/微交互/Toast/骨架屏/滚动条）+ `dist/common.js`（首屏主题、主题切换、统一 Toast/Alert，登录页与后台共享）。
  - 重构 `dist/login.html`（渐变背景 `auth-page`、品牌区 `brand-logo`、聚焦/错误动画、移动端适配）。
  - 优化 `dist/admin.html`（sticky 顶栏、区块标题强调色条 `section-title`、上传区 `upload-area`、卡片精致化），并移除重复主题脚本、冗余第二处 DOMContentLoaded、`updateMapping` 函数，修复 `copyDecodedText` 指向不存在 `#addNewRow` 的问题（改为滚动到 `#newTarget`）。
  - 替换 `index.js` 访客端过期页/微信活码页外观为商务风（保持 no-store 与 404 语义不变）。
  - 同步更新 `docs/CODE_DESIGN.md` 相关章节（3.1/3.2/3.3/4.1/4.18/4.19/4.23/6.5/2.5.3）。
  - 本地用 `python -m http.server` 验证 dist 资源均 200（wrangler 未安装，无法用其预览）。
- 后续 UI 微调：admin.html「二维码识别与短链创建」卡片改为**按钮触发**（主按钮「添加短链二维码」+ `id="createPanel"` 面板默认 `hidden`，点击 `toggleCreateBtn` 切换显隐），避免默认占满全屏。commit `b852335` 已 push。
- 再微调：admin.html「使用说明」卡片也改为**按钮触发**，与「添加短链二维码」按钮并列（`flex flex-wrap gap-3` 一行两按钮，ghost 样式的「使用说明」+ 帮助图标 SVG）；说明内容移入 `id="helpPanel"` 面板默认 `hidden`，点击 `toggleHelpBtn` 切换显隐。仅改 dist/admin.html。
- 两面板（createPanel / helpPanel）改为**互斥展开**：展开其中一个时自动 add('hidden') 收起另一个，再次点同一按钮仍可收起自身（保留 toggle）。
- admin.html 表格「条目名称」表头改为可点击排序按钮（localeCompare 首字母/拼音，升/降切换图标）。commit `bd309c8` 已 push。
- 再扩展：「短链名」表头也支持点击排序，两列**互斥**（点一列重置另一列状态与图标）；排序逻辑通用化为 `sortConfig`/`sortState`（key: name/path，get 取值函数 + 图标常量）。commit `7897177` 已 push。
- 新增「条目全局置顶」功能（作为独立 plan `92a2aea0bd654130964311acd197564a` 执行，已 finished）：
  - 后端 index.js：`initDatabase` 新增 `pinned` 列迁移（`ALTER TABLE ... ADD COLUMN pinned INTEGER DEFAULT 0`，沿用 isWechat/qrCodeData 的 PRAGMA+ALTER 模式，部署后自动生效）；`listMappings` 排序改为 `ORDER BY pinned DESC, created_at DESC`，返回对象补 `pinned`；新增 `pinMapping(path, pinned)` 函数与 `POST /api/mapping/pin` 路由（按 path 更新 pinned，缺 path 返回 400）。
  - 前端 dist/admin.html：新增 `togglePin(path, currentPinned)`（调接口后 `loadMappings()`）；普通行/编辑态/恢复态三处 `.join` 行模板各加「置顶/取消」按钮（`.btn-pin`）+ 置顶行「置顶」badge；手动排序 `applySort` 以 `item[1].pinned` 为首要比较键（置顶项始终置顶，与排序方向无关）。
  - 文档 `docs/CODE_DESIGN.md` 同步：数据模型表加 `pinned` 行、initDatabase 步骤、listMappings 排序与返回结构、新增 2.4.4 `pinMapping` 章节、API 路由 `api/mapping/pin`、数据流写路径。
  - 验证：`node --check index.js` 通过，admin.html 关键改动存在；`wrangler.toml` 的 `env.dev` 已配 D1（dev 库 id `1cfa1ef6-...`），可用 `wrangler dev --env dev` 实跑（未自动跑，属交互式）。
  - 状态：已 commit + push（`2465604`）。
- 自动构建日志（Cloudflare Workers Builds）显示 CI 部署到 `serverless-qrcode-hub-dev`：构建环境 `pnpm@10.11.1`/`nodejs@22.16.0`，`pnpm install --frozen-lockfile` 装依赖，`npx wrangler deploy` 部署，D1 绑定 `qrcode_hub`，定时任务 `0 2 */1 * *`（每2天02:00）。有一个 WARNING：`wrangler.toml` 的 `name="serverless-qrcode-hub"` 与 CI 期望 `serverless-qrcode-hub-dev` 不一致，CI 自动覆盖名部署。已据此在 `README.md` 新增「自动部署（Cloudflare Workers Builds）」小节 + 名称不一致提示。
- 应用户要求，已将 `wrangler.toml` 的 `name` 改为 `serverless-qrcode-hub-dev`（消除 CI 名称不一致 WARNING），并同步更新 `README.md` 名称提示段落（改为泛化说明 + Fork 后按自身 Worker 名修改）。commit+push `6148c8b`（含 README 自动部署小节 + wrangler.toml name + 当日记忆 + 两份计划文件）。
- 新增「条目模糊搜索」功能（plan `92a2aea0bd654130964311acd197564a` 执行中）：
  - 范围：仅「全部」列表；触发：实时输入防抖 300ms（用户明确选择）。
  - 后端 index.js：`listMappings(page, pageSize, search='')` 新增 search 参数，非空时 CTE 加 `AND (name LIKE ? OR path LIKE ?)`，bind `...banPath, '%'+trim+'%', '%'+trim+'%', pageSize, offset`（COUNT 子查询引用同一 CTE 自动收敛）；`/api/mappings` 路由读取 `search=(params.get('search')||'').slice(0,64)` 透传。
  - 前端 dist/admin.html：管理卡片头部按钮组旁新增搜索框（`#searchInput` 带放大镜图标 + `#clearSearchBtn` 清空按钮）；新增 `searchKeyword`/`searchTimer` 状态与 `clearSearch()`/`activateAllView()` 辅助函数；`loadMappings` 在 searchKeyword 非空时拼接 `&search=`；输入 300ms 防抖触发并切回「全部」视图，切「即将过期/已过期」或点清空按钮时清空搜索状态。
  - 文档 `docs/CODE_DESIGN.md` 同步：2.4.1 listMappings 签名与逻辑、API 路由 `/api/mappings`、前端 loadMappings 章节加模糊搜索说明。
  - 验证：`node --check index.js` 通过；admin.html 无 lint 错误。状态：未 commit。
- 后台标题竖排/窄列 bug 修复（用户反馈搜索功能没问题但 UI 改废）：根因是「短链二维码管理」卡片头部 `flex md:flex-row` 在中屏(768-1279px)进入横排但 h2 无 `shrink-0/whitespace-nowrap` 保护，中文被压成 1em 宽竖排。修复：`md`→`xl` 横排断点、标题 `shrink-0 whitespace-nowrap`、按钮组 `shrink-0`、内层 `flex-col sm:flex-row sm:items-center sm:justify-end`、清掉多余 `</div>`。commit+push `0d39fda`。
- 更新 `docs/CODE_DESIGN.md`：4.3 页面结构补 `#searchInput`/`#clearSearchBtn`；4.11 模糊搜索细化（clearSearch/activateAllView 行为）；新增 6.7 后台头部布局修复记录；结尾快照哈希更新为 `0d39fda`。状态：已修改未 commit。
- 时间戳存储改造（expiry + created_at）：后端 index.js 中 expiry/created_at 改为毫秒时间戳（TEXT 列存数字字符串），SQL 比较从 `datetime()` 改为 `CAST(expiry AS INTEGER)`，getExpiringMappings 用 `Date.now()`+3天数值比较，created_at 去掉 DEFAULT CURRENT_TIMESTAMP 改为 JS Date.now() 写入。initDatabase() 新增自动迁移（GLOB 检测旧格式 YYYY-MM-DD 并逐行转为时间戳）。前端 admin.html 新增 dateToTimestamp()/timestampToDateStr()/timestampToInputValue() 三工具函数，覆盖创建/展示/编辑/保存/撤销/过期判断共6处。文档新增 6.3/6.8 节。commit+push `f7f0712`。
- **迁移 bug 修复**：部署后发现 created_at 未转换（仍是 `YYYY-MM-DD HH:MM:SS` 字符串）。根因：原迁移以 expiry 的 GLOB 检测为前置条件，永久链接 expiry=NULL 导致整个迁移块被跳过。修复：将 expiry 和 created_at 迁移拆为独立 Step 1/Step 2，created_at 改用 `LIKE '% %'`（包含空格）检测旧格式，不再依赖 expiry。commit+push `d7e1996`。
- **移动端响应式全面修复**：用户反馈手机访问 UI 挤成一坨。修复内容：
  - 导航栏：标题 `text-lg` 响应式、按钮紧凑化 `btn-xs`、`flex-wrap` 超小屏换行
  - 搜索筛选栏：按钮 `btn-xs md:btn-sm` 响应式，文字 `<span class="hidden sm:inline">` 小屏仅图标
  - 表格操作按钮：每行4按钮添加 SVG 图标 + `btn-icon`/`btn-text` 类，≤768px CSS 隐藏文字仅显示图标
  - **移动端编辑模态框**：≤768px 编辑改为弹窗表单（大输入框 `h-11` + textarea 5行 + toggle + 保存/取消大按钮），替代挤在横向滚动表格里的行内编辑
  - 二维码模态框底部操作区 `flex-col` 纵向排列
  - 上传区域 `p-4 sm:p-6 md:p-8`、分页栏 `btn-xs sm:btn-sm`
  - theme.css 新增 ≤480px 超小屏规则（html font-size 15px 等）
  涉及文件：dist/admin.html、dist/theme.css。commit `fecef4b`，未 push。
- **卡片化 UI 重构**（统一移动端+桌面端卡片布局）：
  - 移除全部 `<table>` / `<thead>` / `<tbody>` 和相关 CSS（`.table-responsive` 等），改为 `#cards-container` 网格容器（`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`）
  - 新增 `createMappingCard()` 替代 `createMappingRow()`：每张卡片显示条目名称（加粗）+ 状态徽章（启用/禁用/微信/置顶）+ 5 按钮 join 组（详情/编辑/删除/二维码/置顶），按钮采用 `btn-icon`/`btn-text` 响应式方案
  - 新增只读 `#detail-modal` 详情模态框 + `showDetailModal()` 函数，展示名称/短链名/URL/日期/状态，含"编辑"按钮可切换到编辑模态框
  - 统一编辑流程：移除 `toggleEditMode()` / `saveEdit()` / `restoreRow()` 三函数，所有编辑统一走 `#edit-modal` 模态框（`openEditModal(originalData)` 不再需要 row 参数，`currentEditRow` 变量移除）
  - 排序按钮从 `<thead>` 迁移到工具栏（`#sortNameBtn` / `#sortPathBtn`）
  - 骨架屏从表格骨架改为 3 列卡片骨架
  - theme.css：`.table-responsive` 滚动条样式 → `.mapping-card` hover 微动效样式
  - `renderCurrentPage()` / `loadMappings()` / `loadExpiringMappings()` 全部更新为卡片容器引用
  - 涉及文件：dist/admin.html（+详情HTML/+排序按钮/-表格/-旧编辑函数）、dist/theme.css（卡片网格样式）。未 commit。
- 技术选型决策：分析了是否需要用框架（React/Vue）重写，结论是当前 2 页面、~1300 行前端的规模不需要。将分析写入 `docs/CODE_DESIGN.md` 新增 6.8 章节（架构决策：为什么不用框架），包含项目规模评估、保持现状理由、Alpine.js 渐进方案、引入框架的信号阈值。同时更新了目录结构和文档尾注。未 commit。
- UI 合并：将卡片条目中的"详情"和"二维码"两个按钮合并为一个"详情"按钮。删除了独立的 `#qr-modal` 弹窗和 `generateQRForMapping()` 函数，把二维码预览区域（容器+URL输入框+logo开关+样式选择+下载按钮）嵌入 `#detail-modal` 下半部分（状态徽章下方分割线后）。`showDetailModal()` 内联了原二维码生成/下载逻辑。卡片按钮从 5 个减为 4 个（详情/编辑/删除/置顶）。元素 id 与 localStorage 键名保持原样，CSS 选择器不变。改动仅在 `dist/admin.html`。未 commit。
